# Ansible

## Requirements

Install asdf and Python plugin

## Getting Started

Install Python:

```
asdf install
```

Install the dependencies:

```
pipenv install
```

Run the playbook:

```
pipenv run ansible-playbook site.yml
```
